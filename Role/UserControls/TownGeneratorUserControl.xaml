<UserControl x:Class="RoleDDNG.Role.UserControls.TownGeneratorUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
             xmlns:converters="clr-namespace:RoleDDNG.Role.Converters"
             xmlns:local="clr-namespace:RoleDDNG.Role.UserControls"
             DataContext="{Binding Source={StaticResource Locator}, Path=TownGenerator}"
             mc:Ignorable="d"
             Background="{DynamicResource {x:Static adonisUi:Brushes.Layer0BackgroundBrush}}">

    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <Grid>
        </Grid>
    </ScrollViewer>
</UserControl>